export interface IEvent {
  id: string;
  name: string;
  price: number;
  time: string;
}

export interface ICard {
  cvc: string;
  expiry: string;
  focused: string;
  name: string;
  number: string;
}
